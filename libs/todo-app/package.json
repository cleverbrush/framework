{
    "name": "@cleverbrush/todo-app",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "start": "tsx src/main.ts",
        "build": "tsup && tsc --project tsconfig.build.json --emitDeclarationOnly",
        "clean": "rm -rf dist tsconfig.build.tsbuildinfo"
    },
    "dependencies": {
        "@cleverbrush/auth": "^1.0.0",
        "@cleverbrush/schema": "^2.0.0",
        "@cleverbrush/server": "^1.0.0",
        "@cleverbrush/server-openapi": "^1.0.0"
    },
    "devDependencies": {
        "tsx": "^4.0.0"
    }
}
